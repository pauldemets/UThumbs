<form method="post" action="{{ path('waiting_user_delete', {'id': waiting_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ waiting_user.id) }}">
    <button class="btn">Delete</button>
</form>
