{% extends 'base.html.twig' %}

{% block title %}WaitingUser{% endblock %}

{% block body %}
    <h1>WaitingUser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ waiting_user.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ waiting_user.name }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ waiting_user.longitude }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ waiting_user.latitude }}</td>
            </tr>
            <tr>
                <th>Destination</th>
                <td>{{ waiting_user.destination }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('waiting_user_index') }}">back to list</a>

    <a href="{{ path('waiting_user_edit', {'id': waiting_user.id}) }}">edit</a>

    {{ include('waiting_user/_delete_form.html.twig') }}
{% endblock %}
